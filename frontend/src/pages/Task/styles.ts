import styled from 'styled-components';

export const Container = styled.div`
  header{
    button {
      display:flex;
      align-items:center;
      background:transparent;
      color: #fff;
      border: none;

      p {
        font-size: 24px;
      }
    }
  }
`;
